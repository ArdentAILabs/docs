---
title: "Rename a project"
sidebarTitle: "Rename project"
description: "Update a project's mutable fields"
openapi: "/openapi.public.json PATCH /v1/projects/{project_id}"
---

Only provided fields are changed. Today the only mutable field is `name`.
