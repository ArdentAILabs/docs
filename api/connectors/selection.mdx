---
title: "Select databases and schemas"
sidebarTitle: "Selection"
description: "Set which discovered paths the connector replicates"
openapi: "/openapi.public.json POST /v1/connectors/{connector_id}/selection"
---

Sets the selected paths from the discovered schema. Requires a completed [discovery](/api/connectors/discover) first — without one, the call fails with `400`.
