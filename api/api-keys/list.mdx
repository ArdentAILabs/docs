---
title: "List API keys"
sidebarTitle: "List keys"
description: "List an organization's active API keys"
openapi: "/openapi.public.json GET /v1/orgs/{org_id}/api-keys"
---

List responses include each key's name, key prefix, scopes, creation time, expiration, and last-used time — never the secret token.
